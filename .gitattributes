# Prediction artifact phải được lưu NGUYÊN BYTE. `_raw_artifact_from_json` và
# `_image_from_json` (src/ocr_bench/prediction.py) băm sha256 file sidecar rồi so
# với giá trị trong JSON — Git đổi CRLF→LF là sha lệch, và mọi bản clone sẽ ném
# PredictionSchemaError thay vì chấm được. Đây là lý do duy nhất repo cần .gitattributes.
calibration/prediction/** -text
prediction/** -text
