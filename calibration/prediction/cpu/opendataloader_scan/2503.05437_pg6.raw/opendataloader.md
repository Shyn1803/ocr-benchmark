with arbitrary λ and arbitrary coefficients c i . They satisfy

$$
U € p € Vs < 1+ X, Hs-1 (
$$

and the parameter λ can be chosen such that the test example has the desired regularity.

# 3.2 Boundary conditions

As in Subsection 2.2 , the coefficients c i and the parameter λ can be used to satisfy homogeneous boundary conditions. Two boundary conditions for both θ = 0 and θ = ω give a homogeneous linear system of 4 equations which has a non-trivial solution iff the determinant vanishes. This condition is used to find again a countable number of values of λ . Let us sketch this approach for the case of Dirichlet boundary conditions and

̸ The condition U (0) = 0 leads to

$$
= (8) i.e C2 + (1 + A)ca)
$$

hence

$$
C3U(3)
$$

The 2 × 2 linear system U ( ω ) = 0 for the coefficients c 3 and c 4 has the determinant

$$
(3.2)
$$

This means that for given angle ω one gets the corresponding exponents λ ∈ C by solving (separately) the two transcendental, scalar equations sin λω = ± λ sin ω . All values Re λ ∈ [ 1 2 , 4] are given for ω k = kπ/ 10, k = 4 , 5 ,..., 20, in [ Dau89 ].

# 3.3 Weak and very weak solutions

As in Subsection 2.3 , the pair ( u,p ) is a weak solution for λ > 0 and a very weak solution for − min(1 ,ξ ) < λ ≤ 0, where

$$
min{ReX 0: X satisfies (3.2)}
$$

in the case of Dirichlet boundary conditions. is defined by

$$
(V . u,9) = 0
$$

The very solution (y,p) € r-lv € is defined by weak

$$

$$

− ∆ v + ∇ q ∈ L 2 (Ω) , ∇ · v ∈ P } , see [ ALP24 ].

