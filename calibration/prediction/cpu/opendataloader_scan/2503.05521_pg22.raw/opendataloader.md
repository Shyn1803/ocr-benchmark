speration function

$$
:= sup L(y)
$$

where the sup is w.r.t. all future directed causal curves γ that connect ( p,x ) and ( q,y ). If there is no such curve, one sets τ (( p,x ) , ( q,y )) = 0. τ B × f F satisﬁes the reverse △ -inequality

$$

$$

For all ( p,x ) , ( q,y ) , ( r,z ) ∈ I × F . In the following we write also τ B × f F = τ . The time seperation function has the following properties: ′ ′

) = 0 if y and y are not cuassally related, ′

) > 0 if y ≪ y .

• The generalized Lorentzian cone w.r.t. − I , the metric space F and the

$$
(I x F,D,<,<,T) =: -I xf F.
$$

If F is an intrinsic metric space, then − I × f F is a Lorentzian pre-length space in the sense of [KS18].

Here we collect some properties of − I × f F . We always assume that F is intrinsic.

F has the push-up property: if (s,2) (t,y) if and only if positive length. Moreover I+/-((s,2)) is open for all (s,t) € I x F. ~Ixf

If F is geodesic; then

• ∈ × The next theorem is the Lorentzian analogue of Theorem 2.5 above.

Theorem 3.1 (Kunzinger, Saemann, Graf, Alexander) . Let X be a strictly intrinsic metric space and let γ = ( α,β ) be a future directed, causal curve that is a maximizer w.r.t. L in − I × f F and parametrized proportional to arclength. Then

(a) β is a minimizer in F ;

(Fiber  independence) Q except for the total height;, i.e More precisely; if Ê is another 0 minimizing geodesic in F with the same length and minimizer in Ê . speed ~IXf

If ~ is timelike; then B has speed

f ◦ α (d) If γ is timelike, then − 1 2 ( α ′ ) 2 + 1 2 f 2 ◦ α = E for a constant E .

We stated above that a generalized cone − I × f F over an intrinsic metric space F is a Lorentzian pre-length space. If F is locally compact, it was shown in [AGKS23] that − I × f F is even a Lorentzian length spaces.

Theorem 3.2. Let F be a locally compact length space. Then − I × f F is a strongly causal Lorentzian length space.

