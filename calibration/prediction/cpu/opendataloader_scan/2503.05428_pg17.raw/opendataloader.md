To complete the proof, our method will be to introduce a certain system of PDEs, then show that this system satisﬁes the Lopatinskii conditions [1], see also [44, Chapter 5, Proposition 11.9], if and only if λ is in the complement of the right side of (49). When the Lopatinskii conditions are satisﬁed, the system is a Fredholm operator which implies M ( λ ) is also Fredholm. Therefore, in this case λ ∈ σ ess ( L ) c which will establish the right inclusion of (49). The Lopatinskii conditions fail if either the system is not elliptic in the interior, or at the boundary. As we will see, interior ellipticity of the system is equivalent to

$$
1 € (53)
$$

We have already shown that failure of this condition leads to existence of a Weyl sequence. Assuming interior ellipticity, we will show that boundary ellipticity is equivalent to

$$
1 € U i|Pn max(0, N2) , max (0, N2
$$

We will show that failure of this condition also leads to existence of a Weyl sequence, which will complete the proof. Let us begin now deriving the PDE system.

For any v ∈ H let us consider the decomposition given by Lemma 2, which can be written as

$$
U = W + T*+
$$

where w ∈ Ker( T ) and ϕ ∈ H 1 ( M ) . Let us further decompose w according the standard Helmholtz decomposition as

$$
W = V x (powv) +
$$

where ϕ v ∈ H 1 ( M ) and the vector potential ρ 0 w v is in the space

$$
X u € n X
$$

while also satisfying

$$
(powv) = 0.
$$

Given that M is a ball, a unique such decomposition exists (see [2, Section 3]). Let us set ρ 0 z v = ∇ ϕ v which must then satisfy

$$
X (pozv) = 0.
$$

Then w ∈ Ker( T ) is equivalent to

$$
(pozv) + 9 X zv = 0, n 0
$$

Now, suppose that u ∈ Ker( T ) satisﬁes

$$
M(AJu = f. (54)
$$

As described above for v , there will be w u and z u such that

where

$$
(powu) + pozu
$$

$$
X =0. (55)
$$

$$
(powu =0 56)
$$

$$
(pozu) + % x (powu) + P9 (57) C2
$$

$$
zulaM = 0 (58)
$$

$$
n X (59)
$$

