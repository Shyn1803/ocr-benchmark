in P ℓ ( D ). We introduce the space of piecewise-polynomial functions

$$

$$

with respect to the partition T h and the space of piecewise-polynomial functions

$$

$$

with respect to the skeleton F h of the mesh T h . The subspace of L 2 ( T h , R m × n ) with components in P ℓ ( T h ) is denoted P ℓ ( T h , R m × n ). Likewise, P ℓ ( F h , R m × n ) stands for the subspace of L 2 ( F h , R m × n ) with components in P ℓ ( F h ). We finally consider

$$

$$

where P ℓ ( ∂K, R m × n ) :=

Remark 2 . It is important to keep in mind that, by definition, the functions in L 2 ( ∂ T h , R m × n ) and P ℓ ( ∂ T h , R m × n ) , are multi-valued on every interior face F , whereas the functions in L 2 ( F h , R m × n ) and P ℓ ( F h , R m × n ) are single-valued on each face F .

=nK is the unit normal vector of ĐK oriented toward the exterior of K. Obviously; if F nK ~nK' on is meaningful by virtue of the trace theorem. For the same reason, if 4 €

F h For k ≥ 0, we introduce the finite-dimensional subspaces of H 1 and H 2 given by

$$
: Pk+1(Th, Rdx2) : H1,h and H2,h
$$

respectively

the following discrete trace inequality.

Lemma 2 . There exists a constant C > 0 independent of h and k such that

$$
/2 (31) Vq € Pk(Th) . hf
$$

For any integer ℓ ≥ 0 and K ∈ T h , we denote by Π ℓ K the L 2 ( K )-orthogonal projection onto P ℓ ( K ). The global projection Π ℓ T in L 2 ( T h ) onto P ℓ ( T h ) is then given by (Π ℓ T v ) | K = Π ℓ K ( v | K ) for all K ∈ T h . Similarly, the global projection Π ℓ F in L 2 ( F h ) onto P ℓ ( F h ) is given, separately for all F ∈ F h , by (Π ℓ F ˆ v ) | F = Π ℓ F (ˆ v | F ), where Π ℓ F is the L 2 ( F ) orthogonal projection onto P ℓ ( F ). In the following, we maintain the notation Π ℓ T to refer to the L 2 -orthogonal projection onto P ℓ ( T h , R m × n ). It should be noted that the tensorial version of Π ℓ T inherently preserves the symmetry of the matrices, as it is derived by applying the scalar operator component-wise. Similarly, we will also use Π ℓ F to denote the L 2 orthogonal projection onto P ℓ ( F h , R m × n ). In the remainder of this section, we provide approximation properties for the projectors defined

In the remainder of this section; we provide approximation properties for the projectors defined above. A detailed of these results can be found in [22, Section 3] and the references therein. proof

Lemma 3 . There exists a constant C > 0 independent of h and k such that

$$
min {r,k}+1 (32) Ilq
$$

