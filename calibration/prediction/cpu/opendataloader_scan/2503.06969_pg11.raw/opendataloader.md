We point out that there are no conditions on spaces and maps, except normality, making the theorem as broad as possible.

We keep the same data as in Lemma 34, 8o On liftcat f (h) So we can work with / and f' = j 0 f rather than with h and f .

By deﬁnition, liftcat f ′ ( µ ) n if and only if there exists a map Φ: X → T ( µ ) making the following diagram homotopy commutative:

![](<2503.06969_pg11_images/imageFile1.png>)

X

> Zh

t n

From Remark 6, we regard the coﬁbrations µ and t n as inclusions and consider that T n ( µ ) =   n i =0 p − 1 i ( A ) ⊆ Z n +1 h where p i is the i -th projection p i : Z n +1 h → Z h .

Assume we have the map $ and the homotopy H: X x I + Zn+1 between f' and tn 0 $. An+1

Consider the open neighborhood N of A in Zh which is deformable in Zh into A with some homotopy G rel A =1 As (tn

Now we can deﬁne homotopies L i : U i × I → Z h by:

$$
p;(H(u;2t)) Li(u,t) = (G(h;(u), 2t _ 1) if  < t < 1.
$$

Each L i is well deﬁned because p i ( H ( u, 1)) = p i (( t n ◦ Φ)( u )) = h i ( u ) = G ( h i ( u ) , 0). ′ ∈ ′ |

Conversely assume we have an open cover ( U i ) 0   i   n of X and homotopies K i : U i × I → Z h with K i ( u, 0) = f ′ ( u ) and K i ( u, 1) ∈ A . Choose a closed subset F i ⊂ U i (possibly the empty set) in each U i . If X is normal, we have two more

$$

$$

X, = = 0 we

Now we deﬁne H i : X × I → Z h by:

$$
if 2 € H;(x,t) = if 2 € Ui.
$$

= f'(x).

Let H Hn): X x I + Zn+1 and $ = H( ,1). We have H( ,0) = 0 f' . On the other hand, for any one i such that Thus H is a homotopy between f' and tn 0 $. An+1 An+1

