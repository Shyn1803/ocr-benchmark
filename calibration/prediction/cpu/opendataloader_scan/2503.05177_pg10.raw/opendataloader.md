From now on we will drop the subscript from the notation C ≤ k,w .

For a sequence s denote by σ ( s ) and d ( s ) respectively its Schnirelmann density and asymptotic density. Namely,

$$
A(n)
$$

and

$$
A(n) d(s) lim if it exists,
$$

where A ( n ) = |{ i : s i ≤ n }| .

Recall Mann’s theorem, relating sumsets to Schnirelmann densities:

Theorem 3.4 (Mann’s Theorem [Man42]) . For subsets A and B of N , if the sumset A + B   = N , then

$$
holds .
$$

We now use Mann’s theorem together with the results above to establish Theorem 1.4.

Proof of Theorem 1.4. By Lemma 3.1 is a tail event, and by Lemma 3.3 we have P(CSk,w) = From EX }neN Introduce the random variable N by Ck;w

$$
A(m) N = min n Vm > n, m
$$

Since   ∞ n =1 P ( N = n ) = 1, it follows that ∃ n 0 such that P ( N = n 0 ) > 0. We now distinguish two cases.

Case 1: 1 ∈ SuppX .

We have

$$
P (N =no for the sequence generated by {X;}izno > 0,
$$

and since 1 ∈ SuppX , we have

$$
P(X1 = Xz = = Xno
$$

Since these two events deal with distinct parts of the i.i.d. sequence { X i } i>n 0 , with positive probability both events occur simultaneously. Now, let us evaluate the Schnirelmann density σ ( { W i } i ≥ 1 ) for such sequences of gaps. We have

$$
if n no A(n) if no n if n 2n0 2n0
$$

  where the ﬁrst two inequalities follow from the choice of the ﬁrst n 0 gaps and the last one from the condition N = n 0 . 1

Thus we get P   σ ( W ) ≥ k   > 0 and Mann’s theorem implies asymptotic ≤ k -completeness with the same positive probability.

Case 2: 1 / ∈ SuppX .

