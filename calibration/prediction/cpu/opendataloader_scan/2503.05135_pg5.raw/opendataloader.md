We have the following subsequent corollary.

Corollary 1. Let C σ be

Yu et al. [18] calculated the positive inertia of signed path

Lemma 6. Let P σ n be a signed path of order n , then p + ( P σ n ) = ⌊ n 2 ⌋ .

Yu et al. [4, Theorem 3.1] characterized all connected signed graph G σ with p + ( G σ ) = 1 .

Lemma 7. Let G σ be a connected signed graph. multipartite signed graph.

The length of a signed path (or simply a path) P σ refers to the total number of edges in P σ . For any two vertices y and z , the distance between them, denoted by d ( y,z ) , is defined as the length of the shortest path between y and z .

Lemma 8 Let Go be 0 connected signed with girth Ir and let Co be If Pơ  of length k from y to y satisfying (V(Pơ) then graph cycle path

$$
Ir k 2
$$

Proof. Remember that C σ have two different paths from y to y ′ . The total length of both the paths equals the length of C σ , i.e., g r . Thus, the shorter length of these paths is at most   g 2 . The shorter of these two paths from y to y ′ , followed by the path P σ , forms a cycle of length at most   g r 2   + k . Therefore, we obtain: g r

$$
Ir 2
$$

$$
+ k 2 gr, Ir k 2
$$

and consequently

This concludes the proof.

and let x be a vertex outside H' . We denote the as:

$$
Ho
$$

Lemma 9. Let G σ be a connected signed graph with girth g r , and let C σ be a shortest cycle in G σ . If

we Nz(Co) # 0. Consequently;

$$

$$

by Lemmas 1 and 2, which contradicts the assumption that therefore, N j ( C σ ) = ∅ for j ≥ 3 . This completes the proof.

Smith [6] showed the following characterization.

Lemma 10. A graph has precisely one positive eigenvalue if and only if the set of its non-isolated vertices constitute a complete multipartite graph.

