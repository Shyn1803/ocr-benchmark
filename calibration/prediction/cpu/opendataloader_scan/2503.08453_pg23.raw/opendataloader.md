Proof. One has to impose d 1 , 1 = 0 and d p,j = 0 in eq. ( 5.7 ). Then, coefficients ω i,j vanish, because all the successive commutators [ K ( h ) ,..., [ K ( h ) ,   K ( h )] ... ] appearing in the BCH formula applied to e K ( h ) e   K ( h ) contain only pure imaginary terms up to order O ( h 2 p +1 ) .

In consequence, if the problem ( 5.1 ) is defined in a Lie group G , then the scheme S h of order p is conjugate to a method that preserves the Lie group structure up to order 2 p +1 .This is so up to order 2 p +2 if Ψ h is symmetric-conjugate and the order p is even, due to the particular structure of K h in that case [ 5 ]. On the other hand, if Ψ h is symmetric-conjugate and its order p is odd, then the resulting AC method is time-symmetric and of order p + 1 .

# 5.2 New alternating-conjugate methods

The analysis of the previous subsection shows that, in addition to concatenating a given method Ψ h of order p (say palindromic or symmetric-conjugate) with the same scheme with complex conjugate coefficients, one can also get an alternating-conjugate method of order p ≥ 2 by considering Ψ h as in Proposition 5.1 , namely by requiring the following order conditions:

$$
1 2 (5.12) ke,j 0
$$

The simplest AC method of order p = 2 corresponds of course to the composition S h = Φ αh Φ αh with

$$
=eK(h) and K(h)
$$

we get α = 1 2 (1 ± i ) , i.e., we recover method ( 5.2 ).

to satisfy the 5 required conditions (5.12). Although there are solutions with Q3 € R, it is more efficient to 5.4) as a 2nd-order time-symmetric method, namely @azh

$$
(5.13) Q1h a2h Qrh
$$

Now the number of order conditions ( 5.12 ) to achieve a method of order 3, 4, 5, 6 is, respectively, 2, 4, 7 and 11. This is the strategy we follow next to construct higher order schemes with the minimum number of basic methods (or stages ). We denote, for brevity, the whole AC method by its sequence of coefficients:

$$
Sh = (Q1,Q2,
$$

Order 3. The two order conditions, ℜ ( k 1 , 1 ) = 1 2 , ℜ ( k 3 , 1 ) = 0 can be satisfied with just one basic scheme, Ψ [2] α 1 h , if

$$
Q1 = 2 2v3
$$

In this way we recover the scheme ( 2.5 ), which is both symmetric-conjugate and AC. Notice that if Ψ [2] h is taken as the Strang splitting ( 2.3 ) for two operators, then the number of exponentials is 5 instead of 12 with a composition of the Lie–Trotter scheme.

