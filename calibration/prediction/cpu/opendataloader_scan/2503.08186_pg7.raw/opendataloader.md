with C" := d IIIEIII-2-4 , and finally

$$
2+4 2
$$

Denoting 9(t) we see therefore that

$$
q (t) 2
$$

which implies

so that in the end

$$
9(t) 2 C" d te-4t
$$

$$
IlT2(t, > e2T t-! .
$$

This yields the claimed L 2 bound.

L ∞ bound, note ﬁrst that the Laplace operator . By the semigroup property, we therefore ﬁnd that

and we use the already established L 2 bound to conclude.

$$
y)llL? (2)
$$

A direct interpolation between the L and L ∞ estimates yields the L estimate Lemma. Finally, the self-adjointness argument leads to the second group of estimates.

In the context of the domains appearing in the proof of Proposition 5 (but keeping the notations of Lemma 11), we write down for the related geometry the following statement:

Lemma 12. Fix   > 0 . Then there exists a constant   ∗  ,  such that for any domain

$$

$$

for R > 0 and % ~ R with |lVollo < Xd = the constant appearing in Lemma 11 is bounded by Rd-1

Proof. For the domain considered in the statement, we need an extension operator   for functions   ∈ H 1 ( Ω ) such that   |   Ω   = 0, for the norms H 1 and L 1 . We can ﬁrst extend   to Ω : =   ′ ,   R   :   >     ′ by setting     = 0 on Ω Ω , and

+ {(   ) ∈   ( )} 1 + \   1   =   on Ω . Recalling that   |   Ω   = 0, we see that ||   1 ||   1 →   1 = 1 and ||   1 ||   1 →   1 = 1. We then use Lemma 10 (with Ω in this lemma corresponding to Ω + here) to build the operator   2 from   1 ( Ω + ) to   1 ( R   ) , so that still thanks to Lemma 10, |||   2   1 ||| = |||   2 ||| ≤ 2   1 + 1 121   2 , and we conclude the Lemma by setting   : =   2   1 .  

We now write down an estimate which is a direct consequence of Lemma 11, and which will be used several times in the sequel.

