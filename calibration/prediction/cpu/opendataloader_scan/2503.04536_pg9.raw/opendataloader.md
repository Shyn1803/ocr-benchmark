o(yi,t + s) as functions of s. These solve Jso(yit + s) = 8,

2.3. Minimization flows and OT. Here we use the approach from [AHT03]. Let T : ( Ω 0 ,ρ 0 ) → ( Ω 1 ,ρ 1 ) be a measure preserving map, i.e.,

$$
Po(x) dx = P1(x) dx T-1(E)
$$

for each Borel set E ⊂ Ω 1 . Set σ t ( · ) = σ ( · , t ) where σ is as before, the flow corresponding to a vector field F satisfying (2.2). The family of maps T ◦ ( σ t ) − 1 :

and

$$
(To (E) = 0t 0 T-'(E), E c Q1
$$

$$
Po(x) dx = dx Po(x) dx since 0 preserves the measure T-1(E) Jp() dx. Po(x)
$$

Consider the function of t

$$
G(t) = Qo
$$

here c   x , y   is a general cost. Making the change of variables x = σ ( z , t ) yields

$$
G(t) = J dz Po(z)
$$

since Po(o(z,t)) J,(z) = JE dx for all t and all E € Qo. If T is an map with respect to the cost c, then G(t) =0 when Po(z) Po(x) optimal

