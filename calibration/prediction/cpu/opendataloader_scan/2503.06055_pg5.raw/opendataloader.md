2.3. Markov Dynamics. If ( X , B ) is a measurable space and U is a topological space, a stochastic kernel P from U to X is a function P : U × B →   + with the property that u  → P ( u,B ) is Borel measurable for each B ∈ B and B  → P ( u,B ) is a probability on ( X , B ) for all u ∈ U . When U = X , we say that P is a stochastic kernel on X . Given a stochastic kernel P from U to X , we set

$$
(h € bmX;
$$

When X is a topological space we use the following standard terminology: We call P Feller if Ph is continuous on U whenever h ∈ bm X is continuous on X . We call P strong Feller if Ph is continuous on U whenever h ∈ bm X .

# 3. ABSTRACT DYNAMIC PROGRAMS

This section is a brief review of the dynamic programming framework from Sargent and Stachurski ( 2025 ). We repeat the key definitions to make the paper partly selfcontained.

3.1. Definition and Properties. An abstract dynamic program (ADP) is a pair ( V,   ), where

(i) V = ( V, ⪯ ) is a poset and

We call V the value space . Each operator T σ in   is called a policy operator . Σ is an arbitrary index set and elements of Σ are called policies . In applications, we impose conditions under which each T σ has a unique fixed point. In these settings, the significance of each policy operator T σ is that its fixed point, denoted below by v σ , represents the lifetime value of following policy σ .

Let ( V,   ) be an ADP with policy set Σ. Given v ∈ V , a policy σ ∈ Σ is called v -greedy if T τ v ⪯ T σ v for all τ ∈ Σ. An element v ∈ V is said to satisfy the Bellman equation if

$$
(v € V).
$$

In ( 1 ), the supremum is taken over all σ ∈ Σ. The Bellman operator generated by

$$
Tv = VTov whenever the supremum exists.
$$

