is of p-measure zero (Here the W € whenever &,B 2 0 for a.e. W € to become 0 the original ones. The existence of the desired W now follows from (+) . key

The following two lemmata may be known; however, we do not know of any references for them.

Lemma 2.2. Let µ be a non-zero ﬁnite (countably additive non-negative) measure on a σ -algebra Σ of subsets of a set Ω , and let X be a Banach space. Let z ∈ S L 1 ( µ,X ) , let E ∈ Σ , and let 0 < ε < 1 . (a) If z ( ω )   = 0 for every ω ∈ E , then there are n ∈ N and pairwise disjoint

(a) If z(w) # 0 for every w € E, then there are n € N and pairwise disjoint measurable subsets Eo, E1 , . ,En of E with U"_0 E; = E such that

    E i     E i i (3) for every i ∈ { 1 ,...,n } with µ ( E i ) > 0 , there is a neighbourhood W i of z in the relative weak topology of B L 1 ( µ,X ) such that

$$
(2.1) w dpl for every W € Wi;
$$

this   neighbourhood can be  chosen to be of the form Wi := {w € (u,X)

There is a neighbourhood W of z in the relative weak topology of B such that

$$
(2.2) dp + € for every w € W . E
$$

Proof. (a). Assume that z ( ω )   = 0 for every ω ∈ E . Choose a real number σ > 0 so that µ ( E \ D ) < ε 2 where D := { ω ∈ Ω:   z ( ω )   > σ } . Since the function z is essentially separably-valued, there is a subset N of D such that µ ( N ) = 0 and the set z ( D \ N ) is separable. It follows that there are pairwise disjoint measurable subsets E i of E , i = 1 , 2 ,... , such that diam z ( E i ) < εσ 2 for every i ∈ N , and D \ N =   ∞ i =1 E i . Pick an n ∈ N so that µ ( C ) < ε 2 where C :=   ∞ i = n +1 E i , and set E 0 := ( E \ D ) ∪ N ∪ C .

Fix an i € {1, ,n} and suppose that   p(E;) 0. Letting € z(Ei) be

$$
Re z* E; = ellzIl) dp = Ei
$$

Now pick a w ∗ i ∈ S X ∗ satisfying Re w ∗ i    ∗

