where the total number of parameters is 1 + m with m ≥ 1. Alternatively, we can express w de as

$$
(zn-1 < z < 2n) = (1 <n < m) ~n-1
$$

Now the parameters become w ( z n )’s, which are values of w de at the divided points and boundaries z n (0 ≤ n ≤ m ) . In this case we have

$$
w( zn_ 3 2n 3{[w(zn_1)~w(o)]+ < z < zn) = e 1 + zn-1_ n=1 3 zi=zi_1 (1 < n < m) i=1 1 + zi_1 F(zn-1
$$

where we have used z 0 = 0. For w de in the last bin z ∈ ( z m , ∞ ), we set it to be a constant w L , and 1 + z

$$
1 F(z > zm) = F(zm)( 8 1 + zm
$$

Now the formula for H ( z ) is ready.

There is one more thing to be mentioned: once we have ﬁtted our model with the data introduced in the next section, errors of w ( z i ) are correlated, i.e., the errors of w ( z i ) are dependent on each other. New parameters can be deﬁned by transforming the covariance matrix of w ( z i ), so that errors of new parameters are decorrelated and do not entangle with each other. The new uncorrelated parameters are referred to as the principal components [10, 23], and they are directly related to their own locations (unlike the correlated case). So errors of the uncorrelated parameters are more interpretable and meaningful. For more discussions and implications of the uncorrelated parameters, we refer to the references [11, 14, 24]. In section IV, we will show both errors of correlated and uncorrelated parameters of w de . The uncorrelated technique we adopt from [11] is as follows.

1.

$$
C = (WWT) = (W)(WT) (9)
$$

where W is the vector of w ( z i ). The Fisher matrix F is deﬁned by F =

# 2. Diagonalize the Fisher matrix by an orthogonal matrix O

$$
F = OTAO , (10)
$$

