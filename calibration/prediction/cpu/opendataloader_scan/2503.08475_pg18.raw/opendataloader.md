We start with the inclusion 2 and hence it is enough to show for n < M aperiodic that Sgen (n) = We first note that since m' and m are not aperiodic, we decompose m = mp + where mp [a,b]p + 4 [a,b]p + 4 [a + 0(p) b + 0(p) - 1]p contained in m. Note that m # mp, since otherwise there exists no m' such that p * m' =M Let n < m be an aperiodic multisegment. There exists np mp obtained via one by that either 1 mod 0(p) or c = 1 Then it is easy to see that there exists n' such that = np + mp and n < m' In this case we have by induction map; map p * n'

$$

$$

If c = 1 obtain that [1,b] = [c;d]p In this case we differentiate 0(p) > 2 and 0(p) = 2. In the former, we let From here the claim follows as in the aperiodic case_ If o(p) = 2 we let m" = m pl-l*p * p * n" = m p * pl-l*p * n" = n and we finish the argument as in the aperiodic case_ we m" again again

It remains to show that W( ρ ×S gen ( m ′ ))⊆S gen ( m ) . Since m ′ cannot be aperiodic, it is enough to show for aperiodic n ⪯ m ′ , ρ ∗ m ′ ⪯ m , which follows from Lemma 2.4.1.

$$
Claim 4: m' * p = m = W(p x Sgen (m' ) ) = Sgen (m)
$$

$$
This can be proven as Claim
$$

×S ( ))=S 1 and Claim 3.

Finally for m ∈M ult R, ◻ , let m = m 1 + ... + m k ∈M ult R be a multisegment with m i ∈M and in pairwise different cuspidal lines. Then we set

$$
Sgen (m) := W(S(m1) * . . . x S(mk) )
$$

and if m ′ = m ′ 1 + ... + m ′ k ∈M ult R, ◻ , m i ∈M ult F ℓ ( ρ i ) is a second product, we define we

$$
m * m' := M1 * mí +_ + mk mk ,
$$

extending the product ∗ to ∗M ult R, ◻ ×M ult R, ◻ →M ult R, ◻ .

Lemma 3.4.2. Let m = m 1 + ... + m k ∈M ult R, ◻ as above. Then

$$
W(Sgen (m) ) ~ Sgen(m1) x X Sgen (mk)
$$

and hence for m ′ ∈M ult R, ◻ a second multisegment

$$
W(Sgen(m) x Sgen(m') ) = Sgen (m * m') .
$$

Note that there exists a non-zero map

$$
Sgen (m1) * _ X Sgen (mk _ W(Sgen (m) ) .
$$

Let π be the unique irreducible subrepresentation of the latter representation. It is then enough to show that π is the unique subrepresentation of the former representation. To see this note that by Theorem 2.2.1 and Lemma 2.2.1, the representation π 1 × ... × π k is irreducible, where π i is the unique irreducible subrepresentation of S gen ( m i ) . Since π 1 × ... × π k is generic, it has to be isomorphic to π . Thus π is a subrepresentation of

$$
Sgen (m1 ) x Sgen (mk ) .
$$

