MULTIPLE SLES FROM CLES 35

depends on the separation δ. Therefore, if for each w ∈ DW,r the marked points of Γ∗,B(w,2r),B(w,r)

out are δ-separated, then the conditional probability that the resampling is FW,r-successful is positive and uniformly bounded from below.

- Lemma 5.4. Fix W ⊆ D and r > 0. For any M0 ∈ N, δ > 0, there exists p > 0 such that the following is true. Suppose that Γ is a multichordal CLEκ in (D;x;β) ∈ Dext2N. Apply the resampling procedure described just above to Γ. Let Eδ be the event that for every w ∈ DW,r we have that

Γ∗,B(w,2r),B(w,r)

out is δ-separated. Then for any choice of target pivotals FW,r = {(ℓ1i,ℓ2i,yi)}i=1,...,M∗ with M∗ ≤ M0 we have

P[GresW,r | Γ]1Eδ ≥ p1Eδ

where GresW,r is the event that the resampling is FW,r-successful.

Proof. Suppose we are given any target pivotals FW,r = {(ℓ1i,ℓ2i,yi)}i=1,...,M∗ with M∗ ≤ M0. If we perform the resampling operation, then with uniformly positive probability (depending on M0,r) we sample M = M∗ and the points wi ∈ DW,r/2 such that wi ∈ B(yi,r/2) for every i = 1,...,M∗. In particular, the strands ℓ1i, ℓ2i cross the annulus A(wi,r,2r). On the event Eδ the strands of Γ∗,B(wi,2r),B(wi,r)

out are δ-separated. Therefore, by Corollary 3.16, the probability that ℓ1i,ℓ2i switch their linking pattern after performing the resampling is bounded from below uniformly in δ. Moreover, by continuity (Theorem 1.7) we see that with positive probability bounded from below the new strands

ℓresi ,1,ℓresi ,2 intersect again inside B(wi,r/2). (Although we have not phrased the topology (1.2) to keep track of the intersection points between the chords, the proof of Theorem 1.7 shows that

continuity also holds when we require the intersection sets to converge.) Repeating this argument M∗ times for each target pivotal, we conclude. □

Recall the setup of Section 4.3. Suppose that D ⊆ C is a simply connected domain and let Γ be a nested CLEκ in D. Let u ∈ (0,1/8) be fixed. Let j0 ∈ Z and z ∈ D be such that B(z,2−j0) ⊆ D.

For j > j0, let Uj = B(z,2−j), Vj = B(z,(1 + u)2−j), and let Gj be the filtration generated by the partially explored CLEκ Γout∗,Vj,Uj. Let φj be the unique conformal map from Vj∗,Uj to D with φj(z) = 0, φ′j(z) > 0. Let (D;sj;βj) be the exterior link pattern decorated marked domain which arises under φj. Let Γsj,βj be the image of Γ under φj. Conditionally on Gj, we have that Γsj,βj is a multichordal CLEκ in (D;sj;βj).

Fix 5/8 < r1 < r2 < 7/8, and let W = A(0,r1,r2). We apply the resampling procedure described above to Γsj,βj with W and r/100. Let (Γsj,βj)resW,r/100 be the multichordal CLEκ arising from the resampling, and let Γresz,j be the CLEκ obtained from mapping back under φ−j 1. Note that any target pivotals FWz,j,r for Γ induce some target pivotals FW,r/100 for Γsj,βj via the map φj. If (Γsj,βj)resW,r/100 is FW,r/100-successful, then Γresz,j is FWz,j,r-successful, according to Definition 5.2. Recalling Lemma 5.4, we now see that Proposition 5.3 is a consequence of the independence across scales Proposition 4.7.

- Lemma 5.5. Fix δ0 > 0, r > 0, and W = A(0,r1,r2). Suppose that (D;x;β) ∈ Dext2N is δ0-separated


- as in Definition 4.3 and let Γ be a multichordal CLEκ in (D;x;β). Let DW,r be as in (5.1). Let Eδ

be the event that for every w ∈ DW,r we have that Γ∗,B(w,2r),B(w,r)

out is δ-separated. Then PCLEκ

(D;x;β)[Eδ] → 1 as δ → 0

- at a rate that depends only on δ0 and r.


