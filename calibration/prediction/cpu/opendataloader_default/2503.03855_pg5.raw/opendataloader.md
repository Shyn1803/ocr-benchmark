THE CANONICAL DIMENSION OF DEPTH-ZERO SUPERCUSPIDAL REPRESENTATIONS 5

for all r ∈ R and α ∈ Φ. We also have a ﬁltration of the maximal torus T by setting T0 := {t ∈ T | ∀χ ∈ X∗(T), ω(χ(t)) = 0}

and

Tr := {t ∈ T0 | ∀χ ∈ X∗(T), ω(χ(t) − 1) ≥ r} for all r ∈ R.

0 1 −1 0

2.1. The apartment. Let N ≤ G be generated by T and ζα

for all

α ∈ Φ. Then N = ZG(T) and the quotient N/T is isomorphic to the (ﬁnite) Weyl group W of G. We write V := X∗(T) ⊗Z R. It is clear that there is an action of W on V by linear transformations. This action can be extended to an action of N on V by aﬃne transformations. When viewing V as an aﬃne space over itself, we denote it by A. It is called the apartment of G corresponding to the torus T. We will ﬁnd it convenient to ﬁx an origin o for A, which corresponds to the origin of

- V . The action of N on A is via the quotient N/T0, which is isomorphic to a group
- W called the aﬃne Weyl group of G. The subset of W acting via aﬃne reﬂections gives rise to a set of aﬃne hyperplanes in A and by taking the closed half-spaces on both sides of all those hyperplanes, we obtain an aﬃne root system Σ in the sense of [3]. Given a ∈ Σ the hyperplane ∂a is called a wall. Every element a ∈ Σ is of the form


a = α + r := {x ∈ A | α(x) + r ≥ 0} for some α ∈ Φ and r ∈ Z and every set of this form is in Σ. Here we are abusing notation by writing α + r both for the aﬃne function x  → α(x) + r on A and for the half-space it deﬁnes. We obtain a surjective map Σ → Φ : a  → aˆ = α. The action of N on A induces an action of N on Σ. Let a,b ∈ Σ, then the walls ∂a and ∂b are called parallel if aˆ = ±ˆb, moreover we say that ∂a is parallel to aˆ.

An equivalence relation on A is now obtained by specifying that two points are equivalent when the sets of aﬃne roots they are contained in are the same. The closures of the equivalence classes under this relation are called facets and they give rise to a simplicial structure on A, where the equivalence classes are the open cells. The simplices of maximal dimension are called alcoves and they are of dimension d. Any alcove is a fundamental domain for the action of N on A. Because we have ﬁxed a pinning of G and a base Π for our root system, there is a canonical alcove C given by

C := {x ∈ A | for 1 ≤ i ≤ d, αi(x) ≥ 0 and α0(x) ≤ 1}.

The vertices of C are v0 = 0 and v1,...,vd. Here the vi with i > 0 are given by αj(vi) = 0 for all j = i,0 and α0(vi) = 1. We now expand α0 in the basis given by Π and ﬁnd that

d

ciαi

α0 =

i=1

for some positive integers ci. We deﬁne ωi := civi and then we note that αi(ωj) = δij. Thus the ωi form a basis for V dual to Π and they are called the fundamental coweights.

Associated to C there is a fundamental Weyl chamber C+ := R≥0 · C. Because C is a fundamental domain for the action of N on A, every vertex x in A is Gconjugate to exactly one of the vi, which gives rise to a map λ : A0 → {0,...,d}

